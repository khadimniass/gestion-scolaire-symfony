<nav class="navbar navbar-expand-lg navbar-light bg-primary">
    <div class="container-fluid">

        <a class="navbar-brand" href="{{ path('app_home') }}">HOME</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarColor03">

            <ul class="navbar-nav w-100 d-flex">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_classe') }}">classe</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_personne') }}">Personnelle</a>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Inscription</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('app_inscription') }}">la liste</a>
                        <a class="dropdown-item" href="{{ path('app_inscription_add') }}">creer une nouvelle</a>
                    </div>
                </li>
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Modules</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('app_module') }}">la liste</a>
                        <a class="dropdown-item" href="#">ajouter nouvelle module</a>
                    </div>
                </li>
                {% if app.user and app.user.roles==["ROLE_AC"] %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_etudiant') }}">Etudiant</a>
                </li>
                {% endif %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Zone reservée</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="{{ path('admin') }}">Administration</a>
                        <a class="dropdown-item" href="{{ path('app_register_rp') }}">nouveau rp</a>
                        <a class="dropdown-item" href="{{ path('app_register_ac') }}">nouveau ac</a>
                    </div>
                </li>
                {% if app.user and app.user.roles==['ROLE_RP']%}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_professeur') }}">Professeur</a>
                        </li>

                    <li class="nav-item align-self-end">
                </li>
                </div>
            </ul>
            <a class="nav-link btn btn-danger " href="{{ path('app_logout') }}">Deconnexion</a>
            {% endif %}
        </div>
    </div>
</nav>