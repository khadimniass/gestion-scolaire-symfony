{% extends 'layout/base.html.twig' %}

{% block title %}Hello PersoneeController!{% endblock %}

{% block body %}

   <table class="table table-hover">
      <tbody>
      <tr class="bg-info">
         <th>#</th>
         <th>Nom Complet</th>
         <th class="text-center">Action</th>
      </tr>
      {% for personne in personnePagiers %}
      <tr>
         <th>{% set i=i+1 %} {{ i }}</th>
         <th>{{ personne.nomComplet }}</th>
         <class="text-center">
            <a href="#" class="btn btn-info">modifier</a>
            <a href="#" class="btn btn-danger">supprimer</a>
          {#  <a href="{{ path('app_categories_show',{id:category.id}) }}">{{ category.mark}}</a>
          #}   <a href="{{ path('app_personne_show', {id:personne.id}) }}" class="btn btn-primary">details</a>
         </th>
      </tr>
      {% endfor %}
      </tbody>
   </table>
   <div class="pagination">
      {{ knp_pagination_render(personnePagiers) }}
   </div>
{% endblock %}
